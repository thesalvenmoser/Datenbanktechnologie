## Data Analysis and Cleaning

### Data Cleaning

The type attribute is the only one needed to build a classifier which returns the type for a given instance. All the other attributes can be removed.
